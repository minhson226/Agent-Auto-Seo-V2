# API Base URL - should point to API Gateway
# In production deployment, this will be the API Gateway service (port 9101 on host, 8080 in container)
# When running dashboard in docker-compose, use: http://api-gateway:8080
VITE_API_BASE_URL=http://localhost:9101

# Environment
VITE_APP_ENV=production

# Application Title
VITE_APP_TITLE=Auto-SEO Dashboard
